#define _GLIBCXX_DEBUG
#include <bits/stdc++.h>
using namespace std;

int main() {
  char in;
  cin >> in;
  if (isupper(in)) {
    cout << 'A' << endl;
  } else {
    { cout << 'a' << endl; }
  }
  return 0;
}